# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
/canopy

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.txt

# Dependency directories
# vendor/

# Go workspace files
go.work
go.work.sum

# Env files
.env
.env.local

# macOS
.DS_Store

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
.worktrees/
.claude/issue-runs/
.claude/settings.local.json
.claude/worktrees/

# canopy demo capture-png outputs — generated on demand, never committed.
# Scope-limited so future legit PNGs (e.g. docs/images/) aren't shadowed.
/*.png
tui/testdata/**/*.png
